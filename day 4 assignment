
select std_subject, sum(mark) as total_marks from student_info group by std_subject;

select max(mark) as maximum_mark, min(mark) as minimum_mark, avg(mark) as average_mark 
from student_info ;

select count(*) as total, category  from food where price > 50
group by category;

result:

# std_subject	  total_marks
    economics	     145
    english	       146
    maths	         101



# maximum_mark	minimum_mark	average_mark
       89	          45	        65.3333


# total	     category
    2	        snacks
    2	        juice
